package com.github.frankreyesgarcia.spoonparser.model;

/**
 * Enumeration of Java element types that can be analyzed.
 */
public enum ElementType {
    CLASS,
    INTERFACE,
    ENUM,
    ANNOTATION,
    METHOD,
    CONSTRUCTOR,
    FIELD,
    PACKAGE
}